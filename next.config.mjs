// next.config.mjs    (ES module style)

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { domains: ['example.com'] },
  // …other options
};

export default nextConfig;
